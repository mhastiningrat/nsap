apiVersion: v1
kind: Service
metadata:
  name: nestjs-api-service
spec:
  type: NodePort
  ports:
    - port: 3033       # The port the service is exposed on
      targetPort: 3033  # The port your application is listening on
  selector:
    app: nestjs-api